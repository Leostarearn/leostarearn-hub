---
title: "Smart Living Guides"
layout: base.njk
permalink: /smart-living/
---

<h1>🌿 Smart Living Guides</h1>
<p>Explore practical wellness, health, and lifestyle tips to improve focus, gut health, energy, and more.</p>

<div class="article-grid">
  {% for post in collections["smart-living"] %}
    <a href="{{ post.url }}" class="article-card">
      <img src="{{ post.data.image }}" alt="{{ post.data.imageAlt or post.data.title }}" loading="lazy"/>
      <div class="article-text">
        <h3>{{ post.data.title }}</h3>
        <p>{{ post.data.description }}</p>
      </div>
    </a>
  {% endfor %}
</div>
